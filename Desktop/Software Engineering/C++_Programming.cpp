#include <iostream>
using namespace std;

// C++ function example
int add_one(int num) {
    int ans = num + 1;
    return ans;
}

int main() {
    int num = 5;
    cout << add_one(num);
    return 0 ;
}